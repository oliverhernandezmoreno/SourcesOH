name: Redundancia de presión de poros

scope: group
groups:
  parents:
    - name: Grupos de validación de piezometros
      canonical_name: validacion-piezometros

inputs:
  - "@ef-mvp/m2/parameters/presion-poros"

meta:
  vars:
    type: array
    items: {type: object}
  heads:
    type: array
    items: {type: object}

script: common-scripts/concentrate.js
